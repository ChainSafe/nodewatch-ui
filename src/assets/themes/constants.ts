import { IConstants } from "@chainsafe/common-theme"

export interface ecColors extends IConstants {
  chartColors: {
    primary: string
    secondary: string
    red: string
    orange: string
    yellow: string
    green: string
    blue: string
    indigo: string
    violet: string
  }
}
